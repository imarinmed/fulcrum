{# Report Page Template - Generates a standard report page with headers and tables #}
{%- macro render_table(headers, rows) -%}
| {{ headers | join(' | ') }} |
|{% for h in headers %}---{% if not loop.last %} |{% endif %}{% endfor %}|
{% for row in rows -%}
| {{ row | join(' | ') }} |
{% endfor -%}
{%- endmacro %}

{%- macro render_header(level, text) -%}
{{ '#' * level }} {{ text }}

{%- endmacro -%}

{# Main content #}
{{ render_header(2, title) }}

{% if headers and rows %}
{{ render_table(headers, rows) }}
{% else %}
No data available
{% endif %}

{% if footer %}
---
{{ footer }}
{% endif %}
